1.客户端连接,在OnWorldLoad时发送SteamID作为name,服务端接收后重置player,转发给所有客户端,重置player,并设置为ghost(可以打开背包界面)


3.在OnEnterWorld时展示UI,UI首次激活发送SteamID作为请求,服务端返回玩家列表信息.
4.每当服务端返回玩家列表信息,更新UIList.

5.客户端在UI创建玩家,发送给SteamID,进行校验,创建并返回ChatMessage,返回玩家列表信息.
6.客户端在UI创建玩家,发送给SteamID,name,尝试删除返回ChatMessage,返回玩家列表信息.

7.在点击play后,发送SteamID,name给服务端,校验是否有相同的name,服务端封装data并分发,客户端接收并load

